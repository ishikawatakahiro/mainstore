<h1>配送済みリスト</h1>

<%= form_tag :action => 'confirm' do %>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Email</th>
      <th>Pay type</th>
	<th>配達済</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>

<% @orders.each do |order| %>
<%   %>
     <tr>
        <td><%= order.name %></td>
        <td><%= order.address %></td>
        <td><%= order.email %></td>
        <td><%= order.pay_type %></td>
<%#        <td><%= check_box "order[]","delivery" %></td>%>
	 <td><%= link_to 'Show', "order" %></td>
       

</td>


      </tr>

<% end %>
  </tbody>

</table>
<br />

<%= link_to 'New Order', new_order_path %>
<%= link_to 'Print', orders_path(@orders, format: 'pdf') %> |

<%= submit_tag "更新"  %>

<% end %>

<p><%= will_paginate @orders %></p>

